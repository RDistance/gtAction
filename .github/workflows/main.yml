name: Deploy to Server

# 定义触发条件
on:
  push:
    branches:
      - main  # 在推送到 `main` 分支时触发

# 定义工作流的环境，设置为 Ubuntu 最新版本
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 第一步，使用 GitHub 提供的 action 来检出仓库代码
    - name: Checkout repository
      uses: actions/checkout@v2

    # 第二步，设置 Node.js 环境
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'  # 使用 Node.js 16 版本

    # 第三步，安装依赖项
    - name: Install dependencies
      run: npm install

    # 第四步，构建项目
    - name: Build project
      run: npm run build

    # 第五步，使用 SSH 部署项目到服务器
    # - name: Deploy to server via SSH
    #   uses: appleboy/ssh-action@v0.1.7
    #   with:
    #     host: ${{ secrets.SSH_HOST }}         # 服务器IP地址，存储在 GitHub Secrets 中
    #     username: ${{ secrets.SSH_USER }}     # 服务器用户名，存储在 GitHub Secrets 中
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}   # SSH私钥，存储在 GitHub Secrets 中
    #     port: 22                             # SSH端口（默认22）
    #     script: |
    #       cd /path/to/your/project            # 服务器上的项目目录
    #       rm -rf *                            # 清空旧文件（根据需求修改）
    #       cp -r ./dist/* .                    # 将新的构建文件复制到项目目录
